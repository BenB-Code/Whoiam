import { WindowActions } from './window-actions';

describe('WindowActions', () => {
  it('should create an instance', () => {
    const directive = new WindowActions();
    expect(directive).toBeTruthy();
  });
});
